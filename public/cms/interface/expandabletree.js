/*
 * expandabletree.js
 * Класс модуля, отвечает за расширение дерева.
 */

/*
 * Функция конструктор     
 */

function ExpandandedTree(){
}

/*
 * 
 */
ExpandableTree.prototype.init = function(){
	
}

/*
 * Добавление элементов  в контейнер
 * @param (array)	children	Массив элементы
 * @param (object) parent	Контейнер для добавления элементов	
 */
ExpandableTree.prototype.appendChildren = function( children, parent ){
	
}